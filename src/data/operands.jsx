const randomIntWithRange = {
  value: {
    name: "Random Integer with Range",
    properties: [
      { value: [100, 200] }
    ],
  },
  html: <div>

  </div>
}

const operands = [
  randomIntWithRange
]

export default operands;
